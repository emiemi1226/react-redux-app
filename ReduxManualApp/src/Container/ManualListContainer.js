/*ßßimport { connect } from "react-redux";
//import api form "../api"; //を作成したら実装をする
//import {} from "../reducers/●●"; //リデューサーを追加したら実装
//import ●● from "../Models";モデルを作成したら実装する

//コンポーネントのインポート
import ManualList from "../Components/ManualList";

const mapDispatchToProps = state => ({...state})

const mapStateToProps = (dispatch,ownProps) => ({
	historyPush: path => history.push(path,ownProps)
	},


)};

export default connect(mapStateToProps,mapDispatchToProps)(ManualList);*/